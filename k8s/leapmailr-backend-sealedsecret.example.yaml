apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: leapmailr-backend-secrets
  namespace: leapmailr
spec:
  encryptedData:
    # Replace these with real encrypted values produced by `kubeseal`.
    DB_PASSWORD: REPLACE_WITH_KUBESEAL_OUTPUT
    JWT_SECRET: REPLACE_WITH_KUBESEAL_OUTPUT
    ENCRYPTION_KEY: REPLACE_WITH_KUBESEAL_OUTPUT
    NR_LICENSE_KEY: REPLACE_WITH_KUBESEAL_OUTPUT
  template:
    type: Opaque
